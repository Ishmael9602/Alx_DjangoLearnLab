from django.urls import path
from . import views

urlpatterns = [
    # placeholders, in case you add views later
    # path('books/', views.list_books, name='list_books'),
    # path('libraries/<int:pk>/', views.library_detail, name='library_detail'),
]
